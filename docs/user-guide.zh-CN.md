> 本文是英文版 [user-guide.md](user-guide.md) 的中文译本，两者不一致时以英文版为准。

# 用户手册（User guide）

本手册讲服务跑起来之后怎么用（安装与模型接入见[部署](deployment.zh-CN.md)）。先在网页界面里走一遍完整的一个任务，从创建任务到生成文档，再介绍示例脚本、其他操作方式与观测台。用的是随附的任务类型「软件需求规格说明」，以及 `examples/library-lending/` 里的示例材料。

用 `scripts/dev.sh` 启动后端与网页界面（见 [deployment.zh-CN.md](deployment.zh-CN.md)），打开 `http://localhost:5680`。

## 1 创建任务

界面是中文的；下文按钮名用界面上的中文，括号里是英文名。页面标题是「需求工作台」。

在任务列表页选择**新建任务**（New task），挑选任务类型，给它起个名字（可选择再加一个领域标签），然后点**新建**（Create）。这个对话框不收文件；新建之后会打开新任务的任务页。

任务页显示任务的状态、完成条件、材料和它的各个会话。材料在**材料清单**（Materials）一栏上传：把文件拖到虚线框里，或者点它选择文件。只收纯文本的 `.md` 或 `.txt` 文件，单个不超过 5 MB。新传的材料要到下一次会话开始时智能体才看得到，所以先传材料，再开会话。

一个任务可以有多个会话（对话），它们都在同一份交付物上工作。点**新建会话**（New session）开一个会话，页面随即进入工作视图。智能体在后台启动，对话区顶上一条简短的状态消息（系统说明）会告诉它这个任务目前的样子和有哪些材料。

## 2 与智能体对话

工作视图分三块：对话区、条目区（交付物，按集合分组）、文档区（材料与已生成的文档）。

告诉智能体你想要什么，例如「请读 inputs 里的材料，整理成需求规格说明。」它会读材料、写条目、然后回复。每条回复先列出它刚做了什么，再说它需要你做什么。需要你处理时，回复末尾会带一张卡片：

| 卡片 | 问的是什么 | 你的选项 |
|---|---|---|
| 提问（Question） | 一个开放式问题 | 打字回答；如果问题针对的是某些条目，可以点**先不管这条**（Keep pending，仅限待定与范围外事项）或**我不知道，你按常识补**（I don't know, fill in from common sense） |
| 选择（Choose） | 从几个答案里选一个 | 点某个选项，或者自己打字回答 |
| 确认（Confirm） | 是否接受这些条目版本 | **确认**（Confirm），或**不对**（Not right，可以附上哪里不对） |
| 建议（Suggestion） | 一个带依据的候选值 | **采纳**（Adopt）或**换一个**（Another one） |
| 提议（Proposal） | 一个计划，说明会新增、改动或删除什么 | **就这样做**（Do it）或**不要**（Don't） |

任何时候都可以不点按钮、直接打字。如果选项里没有你知道的答案，用自己的话说出来就行。

智能体工作期间你仍然可以继续打字；你的消息会在它这一轮结束后一起送达。网页界面目前还没有停止按钮。要中断智能体，用 HTTP 接口发 `stop` 控制请求（见[接口参考](api.zh-CN.md) 5.5 节）：它会清空排队中的消息并中断智能体，已经保存下来的内容不受影响。

## 3 处理条目

每个条目都会显示它的版本号、是否评审通过、以及你是否确认过这个版本。在列表里点一个条目，可以看到每个字段，以及每一部分内容的来源（界面上的来源小标签是下面四种）：

- **材料原文**（Document excerpt）：从某份材料里逐字摘录的一句话，并标明出自哪个文件；
- **用户的话**（Your words）：你在对话里说过的话；
- **执行者补充**（Added by the assistant）：智能体推断或补全的内容，并附上理由；
- **用户直接修改**（Your direct edit）：你自己改动过的字段。

条目下方的按钮可以直接操作：**修改**（Edit）打开字段编辑，**保存为第 N 版**（Save as version N）保存；**删除**（Delete）删掉这个条目；**确认这一版**（Confirm this version）确认当前版本，确认后按钮变成**你已确认 · 撤回**（You confirmed · Withdraw）；**撤销这次修订**（Undo this revision）撤回最近一次修订；**‹ 回到列表**（Back to list）回到条目列表。每次改动都会生成一个新版本，不会覆盖旧内容。如果一个条目在你上次查看之后又被改动过（不管是智能体改的还是另一个页面改的），你的保存会被拒绝，并展示最新版本供你在此基础上重新修改。

编辑一个条目会让你之前对它的确认失效，需要重新确认新版本。每次改动之后，智能体都会告诉你它改了哪些条目，并请你确认新版本。

## 4 完成任务并生成文档

条目列表上方那行汇总（例如「12 个条目，1 个已确认；待定事项 3 条未解决 ▾」）点开后就是完成条件（completion conditions），任务页上也列着同样的完成条件。对随附的这个任务类型来说，每个集合都要满足自己的条件（例如每个用例都已确认，且没有遗留未解决的待定与范围外事项）。只有全部条件都满足，智能体才会把任务标记为完成；完成之后任务变为只读。

点条目区顶上或任务页上的**生成文档**（Generate document）。在弹出的对话框里勾选要包含的条目、为每个条目选定版本，右侧的预览随之变化，点**下载 Markdown**（Download Markdown）保存文件。未经评审或未经确认的版本照样会被包含进去，并在文档里标出。文档是依据任务类型的模板渲染出来的；不经过模型改写。

## 5 跑一遍示例

`examples/library-lending/` 里有一段虚构的学校图书馆需求说明（[`requirements.md`](../examples/library-lending/requirements.md)），以及一个只用 HTTP 接口走完整个流程的脚本。任务服务启动之后（见[部署](deployment.zh-CN.md)）运行：

```bash
examples/library-lending/run.sh http://127.0.0.1:8790
```

脚本会建任务、上传这段说明、开会话、请智能体开始工作、等它回复，最后把生成的文档写到当前目录下的 `srs-<任务编号>.md`。脚本只需要 `curl` 与 `python3`，不必激活虚拟环境。它每 5 秒打印一次智能体的进度；用默认模型跑一遍通常要 1 到 3 分钟，产出 10 到 20 个条目。这段说明有意留了几处没说清，好让智能体有事可问。

脚本建的任务也会出现在网页界面的任务列表里，可以在那里打开它接着做。

## 6 三种操作方式

| 方式 | 命令 |
|---|---|
| 网页（后端加浏览器界面） | `scripts/dev.sh`，然后打开 `http://localhost:5680` |
| 终端客户端 | `python3 -m taskwright_server.create_task tasks/library --name "图书馆" --material examples/library-lending/requirements.md`，再 `python3 -m taskwright_server.chat tasks/library` |
| pi 自带的终端界面 | `python3 -m taskwright_server.tui tasks/library` |

在终端客户端里打字即可对话；`/db` 查看交付物，`/help` 列出其他命令。在 pi 自带的终端界面里输入 `/tw-board` 查看交付物。通过 HTTP 接口脚本化使用的方法见[接口参考](api.zh-CN.md)。

## 7 用观测台查看智能体做了什么

观测台是只读的本地网页应用，不改任何数据。它读任务数据库，以及后端在每次启动 pi 时写下的归档。

```bash
python3 -m taskwright_observatory --runs ./runs/<任务编号> --workspaces ./tasks
# 然后打开 http://127.0.0.1:8770（--port 可改端口；按 Ctrl+C 停止）
```

- `--runs`：任务的归档目录，即直接包含 `pi-events/` 与 `pi-sessions/` 两个子目录的那一层。任务服务在它自己的 `--runs` 目录下为每个任务建一个，所以要传 `./runs/<任务编号>`，不能传 `./runs`：传上一级目录时，观测台找不到任何会话，列表是空的。`--runs` 可以写多次，同时看几个任务，例如看任务服务跑过的全部任务：

  ```bash
  python3 -m taskwright_observatory $(for d in runs/*/; do printf -- '--runs %s ' "$d"; done) --workspaces ./tasks
  ```

- 终端客户端把 `pi-events/` 与 `pi-sessions/` 直接写在 `$TASKWRIGHT_RUNS_DIR` 下（缺省是启动它时所在目录下的 `./runs`），看它的会话时传这个目录本身。在 pi 自带终端界面里跑的会话不产生事件归档，观测台里看不到。
- `--workspaces`：放任务目录的上级目录，也就是任务服务的 `--tasks`。
- 观测台启动时不打印任何内容；命令跑起来之后直接打开上面的地址。
- 文件有变化时会重新读取：跑完新会话，刷新浏览器即可。

| 页面 | 看什么 |
|---|---|
| 会话列表 | 一行一条会话：起止时间、运行次数、轮数、模型请求次数、工具调用次数、被拒次数、结局；每行有「进入会话详情」与「进入任务页」两个链接 |
| 任务 → 任务页 | 任务的过程按阶段排列；点开一个阶段看这段时间的对话和对交付物的每次改动，再点开一轮看每次工具调用的参数、返回与耗时。右侧交付物看板列出每个条目的版本、来源、评审与确认状态，以及完成条件还差哪几项。 |
| 会话详情 | 与任务页相同的视图，只看一条会话 |
| 系统健康 | 核对这套系统本身：配置的工具是否真的加载了、库里的数据与事件是否对得上、被拒的调用后来有没有改正 |
| 概念对照 | 页面上每个词的含义 |

被拒的工具调用会标在它发生的位置，点开那一轮就能看到工具返回的原因。配了 Langfuse 时，每次运行和工具调用还带着到 Langfuse 的链接。

在终端里快速查看：`python3 -m taskwright_observatory.dbshow tasks/<任务编号>` 打印任务、每个条目的各版本与来源、修订和最近的事件；`python3 -m taskwright_observatory.check_db tasks/<任务编号>` 核对库里的数据与事件是否一致。
