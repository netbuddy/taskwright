"""观测台：一个只读的本地网页应用，用来看清楚执行者在一条会话里做了什么。

界面上的层次跟着 pi 走：会话 session → 一次运行 agent run（由一次提示 prompt 触发）→
轮 turn → 模型请求 provider request 与工具调用 tool call。每个词的定义与出处见「概念对照」那一页。

它自己不存任何数据，也不写任何数据。它读三个来源：

1. 任务目录里的任务数据库 `task.sqlite`，一律以只读方式打开。
2. 归档目录里的记录——后端逐行归档的 pi 原始事件流、后端补记、收到时刻索引，
   以及 pi 自己写的会话文件。
3. Langfuse（开源的大模型应用观测平台）。观测台不调它的接口，也不持有它的密钥，
   只按会话编号拼出一个链接，让人点过去看原文。

启动方式见本目录的 README。
"""

__all__ = []
