"""任务服务：给前端的 HTTP 接口（见 docs/api.md）。

只用 Python 标准库。它不写任务数据库：库只由 pi 进程里的工具与扩展命令写，这里只读、拼装、转交。
模块分工：

- errors.py：统一的错误形状与 HTTP 状态码。
- clock.py：把库里的本地时间文字与 pi 会话条目的世界时换成带时区的 ISO 8601。
- library.py：只读地读任务库，拼库事件（约定第 2、3.1 节）、整份数据里任务那一半（4.1）、完成条件（4.2）、条目在各次修订下的内容。
- conversation.py：把 pi 会话文件的当前分支拼成对话记录（4.1 的 conversation）。
- render.py：对交付物的某次修订整体导出，按任务目录里的文档模板渲染成 Markdown（4.3 生成文档）。
- executor.py：每个任务一个执行者看护：启动、续接、切换 pi 会话，从 pi 事件流拼过程与对话类事件（3.2）。
- hub.py：每个任务一个事件分发：SSE 订阅、库事件的转发与兜底轮询、Last-Event-ID 补发。
- app.py：路由与各接口的处理函数。
"""
