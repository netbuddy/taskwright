{
  "name": "taskwright",
  "private": true,
  "version": "0.2.0-alpha",
  "description": "Taskwright: a task-oriented agent that turns a task definition into a reviewed, source-traced deliverable through conversation.",
  "license": "AGPL-3.0-only",
  "workspaces": ["agent", "web", "sim"],
  "engines": { "node": ">=24" },
  "scripts": {
    "preinstall": "node scripts/guard-node-modules.mjs",
    "test": "scripts/test-all.sh",
    "check": "scripts/check-public.sh",
    "dev": "scripts/dev.sh"
  }
}
